/*PRIMITIVE

Boolean
    var yes=true
    var no=false

Indefined
    a variable has undefined value when no value is assigined before using it
    so we can say that undefined means lack of values or unknown value
    var myvar;
    alter(myvar)

String
    var strings="hello world";

null
    var myvar=null;
 
Number
    var int=100;
    var float=100.5;
    var expo=2.56e3;
    var octal=030;


    USER DEFINED
        Objects
        Fuctions
*/

var myvar=78979n;//n=> for bigint
                // if myvar=null=> then myvar will be a object
var variable;   // if not defined then it is undefined
console.log(typeof myvar);

/*
VARIABLES
 Keywords
    let
    const
    var
*/
let myname; // decalreing a variable
myname='amal'; // initialize

